        // this.dynamicWeb3Client = createAlchemyWeb3(alchemyKey);
        // let contractClient = new this.dynamicWeb3Client.eth.Contract(
        //     assetToken.abi,
        //     assetToken.address
        // );

        // contractClient.methods.balanceOf(params).call();
        // return await this.niuralContract.methods.balanceOf(params).call();

        // /**
    //  * Create a sample balanceOf request
    //  * @returns sample balanceOf result
    //  */
    // public static async sampleBalanceOf() {
    //     let web3Client = createAlchemyWeb3(alchemyKey);
    //     let contractClient = new web3Client.eth.Contract(
    //         Assets.MUMBAI.USDS.abi,
    //         Assets.MUMBAI.USDS.address
    //     );

    /*
    let web3Client = createAlchemyWeb3(alchemyKey);
        let contractClient = new web3Client.eth.Contract(
            Assets.MUMBAI.USDS.abi,
            Assets.MUMBAI.USDS.address
        );


        const params = contractClient.methods.balanceOf(
            stramId,
            who
        ).encodeABI()

        return await contractClient.methods.balanceOf(params).call();
    */

    //     const stramId = 100000; // stream id which want to check balance
    //     const who = "0x09549e5243e3717B2cD662a9AC630E1C6687D933"; // address of the party who you want to know the balance

    //     const params = contractClient.methods.balanceOf(
    //         stramId,
    //         who
    //     ).encodeABI()

    //     return await contractClient.methods.balanceOf(params).call();
    // }